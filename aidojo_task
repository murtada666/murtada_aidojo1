def aidojo(number):
    result = []
    for i in range(1,number+1):
        if (i % 3 ==0 and i % 5 ==0):
            i = "AI Dojo"
            result.append(i)

        elif (i % 5 == 0):
            i = "Dojo"
            result.append(i)

        elif(i % 3 == 0):
            i = "AI"
            result.append(i)

        else:
            str_value = "{}".format(i)
            result.append(str_value)

    return result

#main
print(aidojo(3))
print(aidojo(5))
print(aidojo(15))
